1. Requirements
2. Core Entities 
  Entities that you will persist to DB or that your API will exchange. (Eg: User, Ticket, Event etc.)
3. API or Interface
4. Data flow
5. High Level Design 
  Goal: to meet the Functional Requirements of the system
6. Deep dives
  Enhance the High Level Design
  Goal: to meet the Non-functional requirements of the system
----------------------------------------------------------------------------------------------------------------
1. Functional Requirements. 
  Features of the system.
  Like, users should be able to do this thing, that thing etc. 
  Prioritize the top 3 functional requirements.
  Keep your requirements targeted.
  Having a long list of requirements will harm you in the interview.

  Eg: 
  a. Users should be able to post tweets.
  b. Users should be able to follow each other.
  c. Users should be able to see tweets from users they follow.

2. Non-Functional requirements:
  Qualities of the system.
  Characteristics of a system.
  Like, System should be able to handle x requests per second etc. 
  Those are the system qualities that are important to your users.
  Prioritize top 3-5 of non-functional requirements.

  It is better when non-functional requirements are quantified. ("system should be low latency" is obvious, but "system should have low latency search < 500 ms" is meaningful)

  Eg: 
  a. System should be highly available, prioritizing availability over consistency.
  b. System should be able to scale to 100 Million DAUs.
  c. System should be low latency, rendering feeds under 200 ms.

  Ask below questions for getting the proper Non-functional requirements.

  CESL DSF

  1. CAP Theorom
    Should your system prioritize Consistency over Availability ? P stands for Partition.
  2. Environment constraints:
    Like, Are you running on a Mobile device with limited battery life or limited memory or limited bandwidth ?
  3. Scalability:
    All systems need to scale. But does this system need a unique scaling requirement ? Does it have bursty traffic at specific time of the day ? Or peak traffic during holidays ? 
    Also, consider read vs write ratio here. Does your system needs to scale reads or writes more ? 
  4. Latency:
    How quickly does the system needs to respond to user requests ? 
  5. Durability:
    How important is that the data in the system is not lost ? (A social networking application might be able to tolerate some data loss but a banking system cannot)
  6. Security:
    How secure does the system needs to be ? Consider Data Protection, Access Controls  and Compliance with regulation.
  7. Fault Tolerance:
    How well does the system needs to handle failures ? Consider Redundancy, Failover and Recovery mechanisms.
  8. Compliance:

----------------------------------------------------------------------------------------------------------------
Product Design - User facing systems or apps (ticketmaster, bit.ly etc. )
Infra Design - Non-User facing systems (like, message queue, Rate limiter etc. )
----------------------------------------------------------------------------------------------------------------
Fundamentals:
1. Storage
  Should be able to discuss various data storage models and use cases.
  Should be able to articulate when each storage paradigm makes sense based on access patterns and given consistency requirements of the system agreed upon in Non-functional requirements.
  Eg: How data is organized in Relational DB vs Document based DB vs Key-Value Stores
      ACID principles vs BSE principles for distributed databases

2. Scalability
  Its just a concept of how do we scale our system to support more users and more requests.
  Scale compute: 
  i. Horizontal Scaling: Adding more machines to a system to increase its capacity. 
  ii. Vertical Scaling: Adding more resources to a single system to increase its capacity. Bigger Server - More CPU, More RAM

  i. Horizontal Scaling: 
      If we proceed with this type of scaling, then: 
      First challenge: Getting work distributed to the right machine.
      Here, we need to consider how to distribute work across the machines. 
      Work distribution needs to keep the load on the system as even as possible.
        So if one node is receiving more traffic than others, then we are not getting much out of the Horizontal Scaling.
      One way is "Consistent Hashing".
        Here, we arrange data and machines in a circular space called "hash ring", allowing us to add or remove machines with minimal data redistribution.
      Another way is "Load Balancing".
        Here, the load balancer will choose which node to use for an incoming request. 
          It uses differents strategies - like, least connection, utilization-based, round robin etc.


  Scale Storage:
  i. Sharding/Partitioning:
  ii. Consistent Hashing:

3. Networking
  Application Layer (L7)
  a. HTTP/HTTPS protocols
  b. REST vs GraphQL vs gRPC
  c. RESTful Semantics
  d. DNS resolution
  e. Websockets vs SSE (Server sent events)

  Transport Layer (L4) - For Infra style interviews
  a. TCP
  b. UDP
  c. Request response lifecycle

  Network Layer (L3) - Have basic understanding of these
  a. Load Balancing
  b. Firewalls, ACLs

4. Latency, Throughput and Performance
  Goal: To optimize performance of the system
  Should be able to recall approx. latency numbers for common operations.
  Eg: 
    i. Memory access (RAM: 100ns)
    ii. Disc reads (SSD: 0.1-0.2 ms, HDD: 1-2ms)
    iii. Network calls (Same Region: 1-10ms, Cross Region: 10-50 ms)
  Should be able to understand throughput limitations.
  Should be able to perform basic capacity planning estimations.
  Should be able to identify Performance bottlenecks and offering solutions (Eg: Adding Cache to lower latency)
5. Fault tolerance and Redundancy
  Failures are inevitable in distributed systems.
  We should be able to discuss things like Replication strategy, Failure detection mechanisms.
6. CAP Theorom
  Consistency: All users see same data at the same time.
  Availability: System continues to operate even when parts of it fail.
  Partition tolerance: System works despite network failures between nodes.

  System can have only 2 of the above 3. 
  Consistency over Availability ? - choose only one.

STRONG CONSISTENCY: 
  All clients always see the same, most recent value after a write. 

  Consider a situation: 
  An online platform has users in all countries and servers in all countries as well.
  User A writes to a server in Region A.
  User B wants to see the same ticket and his request goes to server in Region B.

  If there is a network issue while replicating data between Region A and Region B, we would have below options:
  1. Stop serving data (affects Consistency)
  2. Risk wrong data (affects Availability)

  Examples where we need strong consistency:
  1. Ticket booking platforms (Airline, Event etc.)
      If we sell a ticket, everyone needs to see the ticket as unavailable, without any delay.
  2. Inventory system (Amazon etc.)
      Can't sell the same last item to multiple customers.
  3. Financial systems 
      Stock trades must be executed in a strict order.

  For above examples, we need a 'single globally agreed state at all times'. 
  1. One system of Record
  2. All transactions go through a leader node.
  3. Synchronous replications 
  4. Locks (When account A is being debited, system acquires a lock on that account)

  Even if a bank has servers in many countries, there is always primary authoritative ledger (often in the headquarter region).
  And, all the writes happen to that primary server.
  And, the local servers have read replicas/cache/edge services (to ensure faster queries and faster user experience).

  Eg: If you withdraw $200 from your account, it will be updated in the main ledger in USA. (when you are withdrawing money from India).   
      But your mobile banking app might still be showing your old balance for a few seconds (as it will be reading from a local replica and the sync may not have happened yet)

  If banks try to synchronize every display read, then viewing balance could take time.
  So, banks will use Strong consistency for writes and Eventual consistency for Reads.
  Because, reading from a local replica is faster, cheaper and scalable.

  And, even if transaction happens in the same country or same building that the bank is present, 
  Writes always go to the main ledger and Reads often come from Replicas. 
  This is done for 
  a. Performance.
  b. Scalability.
  c. Fault tolerance.

  Why not always read from the primary ledger ? 
  1. Because, millions of users open banking apps at the same time.
  2. Reads far outnumber writes. 
  3. Hitting the primary ledger for every read would overload it.


  Here, latency is accepted. Because in banking, correctness is more important than speed. (even if there is cross-region network latency)

CAUSAL CONSISTENCY: 
  Only the operations that are causally related are seen in the same order by everyone. Unrelated operations may be seen in different order. 
  It preserves cause-and-effect, but doesn't force a single global order for everything.
  For chat applications. 
    cause-and-effect must be preserved within the same chat.
    Alice: Hey
    Bob: Hi

    Here, both Alice and Bob has to see Hey -> Hi. 

  For chat applications, we don't want Strong consistency. 
  Because, if we enforce Strong consistency, meaning "All messages in all chats must be seen in the same global order". -> and this works only for banks, tickets etc. 
  And below would be the problems: 
  1. Messaging would slow down.
  2. Every message would require global co-ordination.
  3. One slow region would delay all other chats.

  Eg: You are chatting with Alice in one window and with Bob in another window. 
  Chat A messages can be delivered without waiting for chat B messages.


| Feature                                    | Banking                      | WhatsApp Chat                    |
| ------------------------------------------ | ---------------------------- | -------------------------------- |
| Requires Strong Consistency?               | ✅ Yes                        | ❌ No                             |
| Must maintain global ordering?             | ✅ Yes                        | ❌ Only per conversation          |
| Acceptable Latency?                        | ✅ Yes (200–500 ms OK)        | ❌ No (must be sub-100 ms)        |
| Can system continue during network splits? | ❌ Usually stops writes       | ✅ Continues writing locally      |
| Why?                                       | Prevent financial corruption | Preserve speed & user experience |


  If you don't need strong consistency, then choose availability.
  Eg: Social media platforms. What if user A posts a photo and user B in another country won't see that photo instantly ? It doesn't affect that much effect, and its not a big deal when the delay is for a couple of seconds.
  These can handle data delay for couple of seconds. But in case of consistency, if a ticket is sold, then it has to be shown as sold for all the users instantly. or else, they would try to buy the same ticket.

  1. Social media apps
  2. business review services (like, Yelp)
  3. Streaming platforms (like, Netflix)

  How does choosing Consistency affect my system design ? 
  1. Implement distributed transactions
    If we have a database and a cache, we need to make sure they are strongly consistent.
    If a write happens on a database, it has to happen on cache as well. 
  2. limit to a single node.
    If its a single node, then there won't be any propagation issues.
  3. discuss consensus protocols.
  4. accept higher latency (use loaders etc.)


  How Availability affects system design ? 
  1. Use multiple replicas.

But, important thing to remember is: Different parts of a system can have different requirements.
Eg: TicketMaster
1. Consistency for booking tickets. (We cannot have 2 users booking the same ticket)
2. Availability for CRUD on events. 

Eg: Tinder
1. Consistency for matching. (If user A swipes right to user B, then user B has to see that he is liked by user A instantly).
2. Availability for viewing profile data.
  
----------------------------------------------------------------------------------------------------------------
API Design:

Application Program Interface.
API's enable 2 software components to communicate with each other using a set of definitions and protocols.

Two main protocols:
- REST: Has Http Methods and Resources represented as URL's 
  - Resource: They represent the data that is being acted upon.
  - REST endpoint describe Resources and not Verbs.
  - For naming, use plural Nouns.
  - Here, Resources map directly to core entities;
  - Eg: GET /events
        GET /events/{id}
        GET /events/{id}/tickets
  - Dont use verb in the API endpoint. Verb is always the HTTP method.
    Eg: POST /events/create -> This is wrong.
        POST /events -> This is correct.
- GraphQL: It lets the clients request exactly the data that they need using queries. Reduces over-fetching and under-fetching. 

Server to Server communication: 
- RPC
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Evaluation: 
1. Problem Solving 
  Identify and prioritize the core challenges.
  Eg: In designing a Ticketmaster, focus on booking flow and not the authentication.

2. Solution Design:
  Create scalable architecture with balanced trade-offs

3. Technical Excellence
  Demonstrate deep knowledge and expertise
  Eg: talk about specific areas where things could break

4. Communication
  Clearly explain complex concepts to stakeholders
